# STM32 综合项目进度交接（W25Q64 烧录 → LVGL 读取）

> 本文档给下一个 agent 接手使用。当前进度：**Stage 1（烧录）已完成并实跑通过；Stage 2（LVGL 从 Flash 读资源）待开始**。
> 所有地址/大小数据均来自 2026-08-09 实跑烧录日志（已逐字节校验），可信。

## 一、项目总览

- **目标**：把 LVGL 的字体/图标 `.bin` 从 SD 卡烧进外部 W25Q64 SPI Flash（8MB），之后 LVGL 工程直接读 Flash 显示，**运行时不挂文件系统**。
- **平台**：STM32F103VET6（512KB Flash / 64KB RAM），野火指南者开发板（ILI9341 LCD，FSMC 接口）。
- **关键决策**：FatFS **只做烧录工具**；LVGL 运行时靠 Flash 0x000000 的自定义二级索引，按"地址+大小"直接读资源。

## 二、当前进度（已完成，2026-08-09 实跑通过）

1. 烧录工程（`w25q64烧录（野火官方）`）完成：SD→Flash 逐文件烧录 + **逐字节回读校验** + 写索引表到 0x000000 + 读回 memcmp 验证 + 串口打印全表。
2. **25 个文件全部烧录并校验通过**（串口全 `verify OK!`，末尾 `BURN COMPLETE!`，LED 绿灯）。
3. 索引表已固化在 Flash 0x000000，读回验证 OK。
4. 图标 `.bin` 已全部转换为 LVGL RGB565 格式（20 个，20×20~60×60）。
5. 未做：掉电冷启动只读校验（可选加固）；LVGL 工程读取显示（Stage 2 正题）。

## 三、Flash 布局与 .bin 存储地址（权威数据）

### 总体布局
| 起始地址 | 内容 | 大小 |
|---|---|---|
| 0x000000 | 资源索引表 | 672B |
| 0x002000 | 中文字库 sch16 | 565,991B（占 139 扇区） |
| 0x08D000 | 英文字库（4 个） | 见下表 |
| 0x300000 | 图标区（20 个，各 8KB 槽位） | 160KB |

### 图标区（Area[0]，index 0x300000，20 项，槽位 8KB）
| 索引 | 名称 | Flash 地址 | 实际大小(字节) | 像素 |
|---|---|---|---|---|
| 0 | alarm | 0x300000 | 5004 | 50×50 |
| 1 | refresh | 0x302000 | 1804 | 30×30 |
| 2 | battery | 0x304000 | 804 | 20×20 |
| 3 | bluetooth | 0x306000 | 804 | 20×20 |
| 4 | left | 0x308000 | 7204 | 60×60 |
| 5 | right | 0x30A000 | 7204 | 60×60 |
| 6 | code | 0x30C000 | 1254 | 25×25 |
| 7 | desktop | 0x30E000 | 2454 | 35×35 |
| 8 | fan | 0x310000 | 5004 | 50×50 |
| 9 | folder | 0x312000 | 5004 | 50×50 |
| 10 | gamep | 0x314000 | 5004 | 50×50 |
| 11 | set | 0x316000 | 5004 | 50×50 |
| 12 | lightbulb | 0x318000 | 5004 | 50×50 |
| 13 | paperplane | 0x31A000 | 5004 | 50×50 |
| 14 | music | 0x31C000 | 5004 | 50×50 |
| 15 | play | 0x31E000 | 1804 | 30×30 |
| 16 | power | 0x320000 | 1804 | 30×30 |
| 17 | wifi | 0x322000 | 804 | 20×20 |
| 18 | menu | 0x324000 | 6054 | 55×55 |
| 19 | terminal | 0x326000 | 1254 | 25×25 |

### 中文字库区（Area[1]，index 0x002000，1 项）
| 索引 | 名称 | Flash 地址 | 实际大小(字节) |
|---|---|---|---|
| 0 | sch16 | 0x002000 | 565,991 |

### 英文字库区（Area[2]，index 0x08D000，4 项）
| 索引 | 名称 | Flash 地址 | 实际大小(字节) |
|---|---|---|---|
| 0 | eng_bt16 | 0x08D000 | 257,018 |
| 1 | eng_ui16 | 0x0CC000 | 255,547 |
| 2 | eng_ui28 | 0x10B000 | 274,241 |
| 3 | eng_ui36 | 0x14E000 | 292,486 |

> 注意图标名称：`gamep`（不是 game）、`set`（不是 setting）、`paperplane`（不是 plane），这是实际 .bin 的文件名。

## 四、索引表（二级索引）结构与读取方法

存储位置：Flash **0x000000**，共 **672 字节**：
- `0x000000`：一级 `Index_Group[3]`（72B，每项 24B）
- `0x000048`：二级 `Index_Enter[25]`（600B，每项 24B）

```c
/* 二级索引：每个文件一条 */
typedef struct {
  char     name[16];   /* 资源名，与图标/字体 .bin 同名 */
  uint32_t addr;       /* 该文件在 Flash 的地址 */
  uint32_t size;       /* 该文件实际字节数（不是槽位大小） */
} Index_Enter;

/* 一级索引：每个区域一条 */
typedef struct {
  uint32_t magic;        /* 0xAABBCCDD */
  uint32_t version;      /* 1 */
  uint32_t entry_num;    /* 该区文件数：20 / 1 / 4 */
  uint32_t area_addr;    /* 区起始地址 */
  uint32_t index_offset; /* 该区 index_enter 子表相对 index_enter[0] 的字节偏移 */
  uint32_t total_size;   /* 区预留总大小 */
} Index_Group;
```

三个区域的实际值（已烧录验证）：

| Area | entry_num | area_addr | index_offset | total_size |
|---|---|---|---|---|
| 0 图标 | 20 | 0x300000 | 0 | 512KB |
| 1 中文 | 1 | 0x002000 | 480（=20×24） | 2048KB |
| 2 英文 | 4 | 0x08D000 | 504（=21×24） | 1060KB |

**LVGL 读取逻辑**：读 72B 一级表 → 按 `index_offset/24` 定位子表起点 → 读 `entry_num` 条二级表 → 按 `name` 找资源，拿 `addr + size` → `SPI_FLASH_BufferRead` 读出。

## 五、资源文件格式（已实测确认）

### 图标：LVGL RGB565 裸数据
每个图标 bin = **4 字节头 + RGB565 像素**。4 字节头是打包位域 `uint32_t`（小端）：
```c
cf =  (header) & 0x1F;          /* 全部为 4 = LV_IMG_CF_TRUE_COLOR */
w  = (header >> 10) & 0x7FF;
h  = (header >> 21) & 0x7FF;
数据区大小 = w * h * 2（RGB565）
```
文件大小 = 4 + w*h*2，已全部验证自洽。像素数据为 RGB565（ILI9341 同为 RGB565，可直接显示）。

### 字体：XBF 外部字库格式（5 个字库全部一致）
`.bin` 原始 XBF 布局（无需改，直接按偏移读）：
```
偏移 0        : x_header_t = min(2B) + max(2B) + bpp(1B) + reserved(3B) = 8B
偏移 8        : 位置表 pos_table[]，每个 unicode 4B（uint32），共 (max-min+1) 个
偏移 8+(U-min)*4 : 查 unicode U 的字模位置 pos；pos==0 表示无此字符
pos 处        : glyph_dsc_t = adv_w + box_w + box_h + ofs_x + ofs_y + r = 6B
pos+6 处      : 字模位图，box_w*box_h*bpp/8 字节
```
各字库参数（与 .bin 头 8 字节一致）：
| 字库 | min | max | bpp |
|---|---|---|---|
| sch16 | 0x0042 | 0xF2E0 | 4 |
| eng_bt16 / ui16 / ui28 / ui36 | 0x0030 | 0xF007 | 4 |

**关键**：`my_font_SCH_16.c`（pircture_bin/ch_font/）已经写好了 `__user_font_getdata(offset, size)` 钩子（英文 4 个 .c 同样），当前实现是占位返回 `__g_font_buf`（0 长度数组，不能用）。**LVGL 阶段只需把它改成从 SPI Flash 读**：
```c
static uint8_t __g_font_buf[256];
static uint8_t *__user_font_getdata(int offset, int size){
    SPI_FLASH_BufferRead(__g_font_buf, font_flash_base + offset, size);
    return __g_font_buf;
}
```
注意：位置表很大（ch 约 248KB），**不能整库搬进 RAM**，必须按 offset+size 小块按需读（pos 读 4B、glyph_dsc 读 6B、位图读几十 B）。`__g_font_buf` 小缓冲即可。每个字库一个 `font_flash_base`（sch16→0x002000，eng_bt16→0x08D000，eng_ui16→0x0CC000，eng_ui28→0x10B000，eng_ui36→0x14E000）。

## 六、后续 LVGL 需要做的工作（Stage 2 任务清单）

LVGL 工程路径：`d:\STM32-PROJECT-exercise\STM32综合项目\04.freeRTOS+LVGL+flash字库\`
（当前 main.c 是 hello 按钮 demo，还没集成 Flash 读取）

1. **移植 SPI Flash 驱动**：把烧录工程的 `User/flash/bsp_spi_flash.c/h` 拷进 LVGL 工程（SPI1：PA5/6/7 + CS=PC0）。LVGL 工程目前没有 Flash 驱动。
2. **读索引表**：0x000000 读 672B，按第四节逻辑定位 25 个资源。
3. **图标显示**：按 name 查索引 → 读出 ≤8KB → 构造 `lv_img_dsc_t{header.cf=LV_IMG_CF_TRUE_COLOR, .header.w, .header.h, .data=像素}` → `lv_img_set_src(img, &dsc)`。
4. **字体集成**：把 `my_font_SCH_16.c`（以及 4 个英文 .c）加进 LVGL 工程，实现 `__user_font_getdata()` 从 SPI Flash 读（第五节），然后正常用 `lv_label_set_font()` / `lv_obj_set_style_text_font()`。
5. **RAM 预算**：VET6 64KB。LV_MEM pool 32KB（lv_conf.h）。图标 ≤8KB 整体读出放 RAM 没问题；字体绝不可整库读入，走按需读。

## 七、注意事项 / 踩坑记录

1. **文件大小必须 ≤ 分配的槽位**（否则写穿到下一文件，会静默损坏且校验不报错）。当前全部满足（图标最大 7204B<8KB，字体均<扇区数）。
2. **RGB565 字节序**：若图标显示颜色红蓝对调，检查 `LV_COLOR_16_SWAP` 是否匹配转换时的设置。
3. **烧录工程 aux_data.c 是纯 ASCII**（曾因中文编码损坏整体重写），勿再改回中文注释/字符串。
4. **SD 卡结构**（重烧时需要）：根目录 `icon_bin/`、`ch_font/`、`en_font/` 三文件夹，只放 .bin。源文件在 `d:\STM32-PROJECT-exercise\STM32综合项目\pircture_bin\`。
5. **重新烧录**：改烧录工程 `User/aux_data.c` 的 `burn_data[]` 后重新编译烧录即可；只改 LVGL 侧则无需重烧。
6. **FatFS 只活在烧录工程**；LVGL 运行时不要挂文件系统（随机读、RAM、簇链开销都不合适）。
7. 串口开头可能有一条 `W25Q64 ID\0■` 乱码——首次上电复位残留，无害。

## 八、关键文件路径

- 烧录工程：`d:\STM32-PROJECT-exercise\STM32综合项目\w25q64烧录（野火官方）\`
  - `User/aux_data.c`（burn_data 表 + 烧录 + 索引写回）、`User/aux_data.h`（结构体定义）、`User/main.c`（烧录主流程）
  - `User/flash/bsp_spi_flash.c`（SPI 驱动，LVGL 需移植）
- LVGL 工程：`d:\STM32-PROJECT-exercise\STM32综合项目\04.freeRTOS+LVGL+flash字库\`（待改）
- 资源源文件：`d:\STM32-PROJECT-exercise\STM32综合项目\pircture_bin\`
  - `icon_bin/`、`ch_font/my_font_SCH_16.c`、`en_font/my_font_ENG_*.c`（XBF 字库定义，含 `__user_font_getdata` 钩子）

## 九、建议的第一步

在 LVGL 工程做最小冒烟测试（推荐顺序）：
1. 拷 bsp_spi_flash.c/h 进 LVGL 工程，SPI 初始化 + `SPI_FLASH_ReadID()` 打印确认 0xEF4017。
2. 读 0x000000 索引表，串口打印（复现烧录工程 `burn_index_table` 的打印逻辑）。
3. 先显示一个图标（如 alarm，0x300000，5004B，50×50 RGB565）。
4. 再切字体显示中文/英文。
